/**
 * Genesis backend API base URL (includes /api prefix).
 * 10.0.2.2 is the Android emulator alias for the host machine's localhost.
 * For physical devices, replace with your machine's LAN IP address.
 */
export const GENESIS_API_BASE_URL = 'http://10.0.2.2:8000/api';
